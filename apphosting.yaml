runConfig:
  maxInstances: 1
  minInstances: 0
  concurrency: 80
  cpu: 1
  memoryMiB: 512

scripts:
  buildCommand: npm run build
  runCommand: npm start

outputFiles:
  serverApp:
    include:
      - dist
      - server.mjs
